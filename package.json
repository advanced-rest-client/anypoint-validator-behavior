{
  "name": "anypoint-validator-behavior",
  "version": "0.1.1",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "Implements a custom input or form validator for the Anypoint components",
  "devDependencies": {
    "polymer-cli": "^0.16.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "anypoint-validator-behavior.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint anypoint-validator-behavior.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"anypoint-validator-behavior:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/anypoint-validator-behavior.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/anypoint-validator-behavior/issues",
    "email": "arc@mulesoft.com"
  }
}